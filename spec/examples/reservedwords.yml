_desc: Reserved words in table and column names

_input: |
  CREATE TABLE "where" (
    nickname CHARACTER VARYING(255),
    "false" CHARACTER VARYING(255),
    "else" NUMERIC(5,2)
  )

table_name: where
columns.length: 3
columns.map(&:name):
  - nickname
  - "false"
  - else
columns.map(&:options):
  - CHARACTER VARYING(255)
  - CHARACTER VARYING(255)
  - NUMERIC(5,2)

to_sql: &standard |
  CREATE TABLE "where" (
    nickname CHARACTER VARYING(255),
    "false" CHARACTER VARYING(255),
    "else" NUMERIC(5,2)
  )
to_mysql: *standard
to_postgresql: *standard
to_sqlite3: *standard
