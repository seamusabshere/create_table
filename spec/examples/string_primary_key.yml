_desc: String primary key

_input:
  - |
    CREATE TABLE cats (
      nickname CHARACTER VARYING(255) PRIMARY KEY,
      birthday DATE,
      license_id INTEGER,
      price NUMERIC(5,2)
    )
  - |
    CREATE TABLE cats (
      nickname CHARACTER VARYING(255),
      birthday DATE,
      license_id INTEGER,
      price NUMERIC(5,2),
      PRIMARY KEY (nickname)
    )

table_name: cats
columns.length: 4
columns.map(&:name):
  - nickname
  - birthday
  - license_id
  - price
columns.map(&:options):
  - CHARACTER VARYING(255) PRIMARY KEY
  - DATE
  - INTEGER
  - NUMERIC(5,2)

to_sql: &standard |
  CREATE TABLE cats (
    nickname CHARACTER VARYING(255) PRIMARY KEY,
    birthday DATE,
    license_id INTEGER,
    price NUMERIC(5,2)
  )
to_mysql: *standard
to_postgresql: *standard
to_sqlite3: *standard
