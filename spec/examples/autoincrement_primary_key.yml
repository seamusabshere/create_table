_desc: Auto-incrementing primary key defined inline

_input:
  - |
    CREATE TABLE cats (
      id INTEGER PRIMARY KEY AUTO_INCREMENT,
      nickname CHARACTER VARYING(255),
      birthday DATE,
      license_id INTEGER,
      price NUMERIC(5,2)
    )
  - |
    CREATE TABLE cats (
      id INTEGER AUTO_INCREMENT,
      nickname CHARACTER VARYING(255),
      birthday DATE,
      license_id INTEGER,
      price NUMERIC(5,2),
      PRIMARY KEY ("id")
    )
  - |
    CREATE TABLE cats (
      id INTEGER PRIMARY KEY AUTO_INCREMENT,
      nickname CHARACTER VARYING(255),
      birthday DATE,
      license_id INTEGER,
      price NUMERIC(5,2)
    )

table_name: cats
columns.length: 5
columns.map(&:name):
  - id
  - nickname
  - birthday
  - license_id
  - price
columns.map(&:options):
  - INTEGER PRIMARY KEY AUTO_INCREMENT
  - CHARACTER VARYING(255)
  - DATE
  - INTEGER
  - NUMERIC(5,2)
columns.first.autoincrement: true

to_sql: &mysql_and_postgresql |
  CREATE TABLE cats (
    "id" INTEGER PRIMARY KEY AUTO_INCREMENT,
    nickname CHARACTER VARYING(255),
    birthday DATE,
    license_id INTEGER,
    price NUMERIC(5,2)
  )
to_mysql: *mysql_and_postgresql
to_postgresql: *mysql_and_postgresql
to_sqlite3: |
  CREATE TABLE cats (
    "id" INTEGER PRIMARY KEY AUTOINCREMENT,
    nickname CHARACTER VARYING(255),
    birthday DATE,
    license_id INTEGER,
    price NUMERIC(5,2)
  )