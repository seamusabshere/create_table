_desc: MySQL-style backticks with TEMPORARY

_input: |
  CREATE TEMPORARY TABLE `cats` (
    `nickname` CHARACTER VARYING(255),
    `birthday` DATE,
    `license_id` INTEGER,
    `price` NUMERIC(5,2)
  )

table_name: cats
temporary: true
columns.length: 4
columns.map(&:name):
  - nickname
  - birthday
  - license_id
  - price
columns.map(&:options):
  - CHARACTER VARYING(255)
  - DATE
  - INTEGER
  - NUMERIC(5,2)

to_sql: &standard |
  CREATE TEMPORARY TABLE cats (
    nickname CHARACTER VARYING(255),
    birthday DATE,
    license_id INTEGER,
    price NUMERIC(5,2)
  )
to_mysql: *standard
to_postgresql: *standard
to_sqlite3: *standard