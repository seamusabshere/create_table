name: MySQL-style backticks with TEMPORARY
table_name: cats
temporary: true
columns.length: 4
columns.map(&:name):
  - nickname
  - birthday
  - license_id
  - price
columns.map(&:options):
  - CHARACTER VARYING(255)
  - DATE
  - INTEGER
  - NUMERIC(5,2)
input: |
  CREATE TEMPORARY TABLE `cats` (
    `nickname` CHARACTER VARYING(255),
    `birthday` DATE,
    `license_id` INTEGER,
    `price` NUMERIC(5,2)
  )
output: |
  CREATE TEMPORARY TABLE cats (
    nickname CHARACTER VARYING(255),
    birthday DATE,
    license_id INTEGER,
    price NUMERIC(5,2)
  )
