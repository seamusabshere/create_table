_desc: Index defined in the "table constraints" section

_input:
  - |
    CREATE TABLE cats (
      nickname CHARACTER VARYING(255),
      birthday DATE,
      license_id INTEGER,
      price NUMERIC(5,2),
      KEY index_cats_on_nickname (nickname)
    )
  - |
    CREATE TABLE cats (
      nickname CHARACTER VARYING(255),
      birthday DATE,
      license_id INTEGER,
      price NUMERIC(5,2),
      INDEX index_cats_on_nickname (nickname)
    )

table_name: cats
columns.length: 4
columns.map(&:name):
  - nickname
  - birthday
  - license_id
  - price
columns.map(&:options):
  - CHARACTER VARYING(255)
  - DATE
  - INTEGER
  - NUMERIC(5,2)
indexes.length: 1
indexes.select(&:unique).length: 0
indexes.map(&:column_names):
  -
    - nickname

to_sql: &standard
  - |
    CREATE TABLE cats (
      nickname CHARACTER VARYING(255),
      birthday DATE,
      license_id INTEGER,
      price NUMERIC(5,2)
    )
  - CREATE INDEX index_cats_on_nickname ON cats (nickname)
to_mysql: *standard
to_postgresql: *standard
to_sqlite3: *standard
